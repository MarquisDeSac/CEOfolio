---
import { getCollection } from 'astro:content'
import BaseLayout from '~/layouts/BaseLayout.astro'
import { resolvePath, withBasePath } from '~/utils/path'

export async function getStaticPaths() {
  const projects = await getCollection('projects')
  return projects.map(project => ({
    params: { id: project.id },
    props: { project },
  }))
}

const { project } = Astro.props
const { data } = project

// Multi-language Data Selection
const desc = data.desc_en || data.desc
const details = data.details_en || data.details
const skills = data.skills_en || data.skills
const category = data.category_en || data.category

const colors = ['neon-blue', 'neon-pink', 'sunset-purple']
// Deterministic hash for consistent color
const hash = project.id.split('').reduce((acc, char) => acc + char.charCodeAt(0), 0)
const color = colors[hash % 3]

const colorMap = {
  'neon-blue': '0, 243, 255',
  'neon-pink': '255, 0, 127',
  'sunset-purple': '188, 19, 254'
}
const rgbColor = colorMap[color as keyof typeof colorMap]

const title = data.id.split('-').map(word => word.charAt(0).toUpperCase() + word.slice(1)).join(' ')
---

<BaseLayout title={title} description={desc} bgType="city-lights">
    <style define:vars={{ 'c-main': rgbColor }}>
      :root {
        --c-main: var(--c-main);
      }
    </style>

    <div class="max-w-5xl mx-auto px-6 py-10 relative z-10">
        <!-- Back Button -->
        <a href={resolvePath('/en/projects')} class="inline-flex items-center gap-2 text-sm opacity-60 hover:opacity-100 hover:text-black dark:hover:text-white transition-all mb-8 group no-underline u-text-main">
            <div class="i-ri-arrow-left-line group-hover:-translate-x-1 transition-transform"></div>
            Back to Projects
        </a>

        <!-- Custom Header (Icon & Title) -->
        <header class="flex flex-col md:flex-row gap-8 items-start mb-12">
            <div class={`p-8 rounded-2xl bg-${color}/10 border border-${color}/20 text-${color} text-6xl shadow-[0_0_30px_rgba(var(--c-${color}),0.15)]`}>
                <div class={data.icon}></div>
            </div>
            <div class="flex-1">
                <div class="flex flex-wrap items-center gap-3 mb-4">
                    <span class={`px-3 py-1 rounded-full text-xs font-bold bg-${color}/10 text-${color} border border-${color}/20 uppercase tracking-wider`}>
                        {category}
                    </span>
                    {data.date && (
                        <span class="text-sm opacity-50 flex items-center gap-1 u-text-main">
                            <span class="i-ri-calendar-line"></span> {data.date}
                        </span>
                    )}
                </div>
                <h1 class="text-4xl md:text-5xl font-display font-bold mb-4 leading-tight u-text-main">{title}</h1>
                {data.client && (
                    <p class="text-xl opacity-60 font-mono u-text-main">{data.client}</p>
                )}
                {data.projectType === 'semi-pro' && (
                    <div class="mt-3 flex items-center gap-2">
                        <span class="px-3 py-1 rounded-full text-xs font-bold bg-amber-500/10 text-amber-400 border border-amber-500/20 uppercase tracking-wider flex items-center gap-1.5">
                            <span class="i-ri-building-line"></span> Tutored Project — Real Client
                        </span>
                    </div>
                )}
                {data.recommendation && (
                    <div class="mt-2 flex items-center gap-2">
                        <span class="px-3 py-1 rounded-full text-xs font-bold bg-emerald-500/10 text-emerald-400 border border-emerald-500/20 uppercase tracking-wider flex items-center gap-1.5">
                            <span class="i-ri-award-line"></span> Letter of recommendation
                        </span>
                    </div>
                )}
            </div>
        </header>

        <!-- Main Content Grid -->
        <div class="grid grid-cols-1 md:grid-cols-3 gap-10">
            
            <!-- Left: Description & Details -->
            <div class="col-span-1 md:col-span-2 space-y-8">
                <section class="prose max-w-none u-text-main">
                    <h3 class="text-xl font-bold mb-4 flex items-center gap-2 u-text-main">
                        <span class={`i-ri-file-text-line text-${color}`}></span> About
                    </h3>
                    <p class="text-lg leading-relaxed">
                        {desc}
                    </p>
                </section>

                {(details || []).length > 0 && (
                    <section>
                        <h3 class="text-xl font-bold mb-4 flex items-center gap-2 u-text-main">
                            <span class={`i-ri-list-check text-${color}`}></span> Key Features
                        </h3>
                        <ul class="grid gap-3">
                            {(details || []).map(detail => (
                                <li class="flex items-start gap-3 p-4 rounded-lg bg-black/5 dark:bg-white/5 border border-black/5 dark:border-white/5 hover:border-black/10 dark:hover:border-white/10 transition-colors">
                                    <span class={`i-ri-check-line text-${color} mt-1 shrink-0`}></span>
                                    <span class="text-sm leading-relaxed u-text-muted">{detail}</span>
                                </li>
                            ))}
                        </ul>
                    </section>
                )}
            </div>

            <!-- Right: Skills & Meta -->
            <div class="col-span-1 space-y-8">
                <section class="p-6 rounded-xl bg-black/5 dark:bg-white/5 border border-black/5 dark:border-white/10">
                    <h3 class="text-sm font-bold opacity-50 uppercase tracking-widest mb-4 u-text-main">Skills</h3>
                    <div class="flex flex-wrap gap-2">
                        {(skills || []).map(skill => (
                            <span class="px-3 py-1.5 text-xs rounded border border-black/10 dark:border-white/10 bg-white dark:bg-black/20 u-text-muted hover:border-black/30 dark:hover:border-white/30 transition-colors">
                                {skill}
                            </span>
                        ))}
                    </div>
                </section>

                <section>
                    <a href={data.link} target="_blank" class={`flex items-center justify-center gap-2 w-full p-4 rounded-xl bg-${color} text-black font-bold hover:brightness-110 transition-all shadow-[0_0_20px_rgba(var(--c-${color}),0.4)] hover:shadow-[0_0_30px_rgba(var(--c-${color}),0.6)] no-underline`}>
                        <span class="i-ri-github-fill text-xl"></span>
                        View Repository
                    </a>
                </section>

                {data.playLink && (
                    <section>
                        <a href={data.playLink} target="_blank" class="flex items-center justify-center gap-2 w-full p-4 rounded-xl bg-emerald-500 text-black font-bold hover:brightness-110 transition-all shadow-[0_0_20px_rgba(16,185,129,0.4)] hover:shadow-[0_0_30px_rgba(16,185,129,0.6)] no-underline animate-pulse hover:animate-none">
                            <span class="i-ri-gamepad-line text-xl"></span>
                            {data.playLabel_en || 'Play'}
                        </a>
                    </section>
                )}

                {data.recommendation && (
                    <section class="p-4 rounded-xl bg-amber-500/5 border border-amber-500/20">
                        <div class="flex items-center gap-2 mb-3">
                            <span class="i-ri-award-fill text-amber-400 text-lg"></span>
                            <h3 class="text-sm font-bold text-amber-400 uppercase tracking-widest">Recommendation</h3>
                        </div>
                        <a href={withBasePath(data.recommendation)} download="Recommendation_Letter_ClassLab.pdf" target="_blank" class="flex items-center justify-center gap-2 w-full p-3 rounded-lg bg-amber-500/10 border border-amber-500/20 text-amber-400 font-bold hover:bg-amber-500/20 transition-all no-underline text-sm">
                            <span class="i-ri-download-2-line text-lg"></span>
                            Download the letter
                        </a>
                    </section>
                )}
            </div>
        </div>

        <!-- Media Gallery -->
        {(data.video || (data.images && data.images.length > 0)) && (
            <div class="mt-16 border-t border-black/10 dark:border-white/10 pt-10">
                <h2 class="text-2xl font-display font-bold mb-8 flex items-center gap-3 u-text-main">
                    <span class={`i-ri-image-line text-${color}`}></span> Gallery
                </h2>
                
                {/* Recommendation Letter Viewer */}
                {data.recommendation && (
                    <div class="mb-10">
                        <h3 class="text-xl font-bold mb-4 flex items-center gap-2 u-text-main">
                            <span class="i-ri-award-fill text-amber-400"></span>
                            {data.recommendation_label_en || 'Letter of recommendation'}
                        </h3>
                        <div class="w-full rounded-2xl overflow-hidden bg-black/5 dark:bg-white/5 border-2 border-amber-500/30 shadow-[0_0_30px_rgba(245,158,11,0.1)] relative">
                            <div class="absolute top-0 left-0 w-full h-1 bg-gradient-to-r from-amber-500 to-amber-300"></div>
                            <iframe 
                                src={withBasePath(data.recommendation)} 
                                class="w-full h-[70vh] min-h-[500px]"
                                title="Letter of recommendation"
                            ></iframe>
                            <div class="p-4 bg-black/10 dark:bg-white/5 border-t border-amber-500/20 flex items-center justify-between">
                                <span class="text-sm opacity-60 u-text-muted flex items-center gap-2">
                                    <span class="i-ri-file-pdf-2-line text-red-400"></span>
                                    PDF — {data.recommendation_label_en || 'Letter of recommendation'}
                                </span>
                                <a href={withBasePath(data.recommendation)} download="Recommendation_Letter_ClassLab.pdf" class="flex items-center gap-2 px-4 py-2 rounded-lg bg-amber-500/10 border border-amber-500/20 text-amber-400 font-bold hover:bg-amber-500/20 transition-all no-underline text-sm">
                                    <span class="i-ri-download-2-line"></span>
                                    Download
                                </a>
                            </div>
                        </div>
                    </div>
                )}
                
                <div class="flex flex-col gap-8">
                    {/* Video Demo */}
                    {data.video && (
                        <div class="w-full">
                            <p class="text-xs text-center opacity-50 mb-2">If the video does not play, please refresh the page (F5)!</p>
                            <div class="w-full rounded-2xl overflow-hidden bg-black/5 dark:bg-white/5 border border-black/10 dark:border-white/10 shadow-lg">
                                <video 
                                    controls 
                                    autoplay 
                                    loop 
                                    muted 
                                    playsinline 
                                    poster={data.images && data.images.length > 0 ? withBasePath(data.images[0]) : undefined}
                                    class="w-full h-auto max-h-[70vh] object-contain"
                                >
                                    <source src={withBasePath(data.video)} type="video/mp4" />
                                    Your browser does not support the video tag.
                                </video>
                            </div>
                        </div>
                    )}

                    {/* Image Grid */}
                    {data.images && data.images.length > 0 && (
                        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                            {data.images.map((img: string, i: number) => (
                                <button 
                                    data-src={withBasePath(img)} 
                                    class={`lightbox-trigger block group rounded-xl overflow-hidden bg-black/5 dark:bg-white/5 border border-black/10 dark:border-white/10 relative hover:border-${color} transition-all duration-300 hover:shadow-[0_0_20px_rgba(var(--c-${color}),0.2)] aspect-video w-full cursor-pointer`}
                                >
                                    <img 
                                        src={withBasePath(img)} 
                                        alt={`Screenshot ${i + 1} of ${title}`}
                                        loading="lazy"
                                        class="w-full h-full object-cover transform group-hover:scale-105 transition-transform duration-500"
                                    />
                                    <div class="absolute inset-0 bg-black/50 opacity-0 group-hover:opacity-100 transition-opacity duration-300 flex items-center justify-center">
                                        <span class="i-ri-zoom-in-line text-white text-3xl opacity-0 group-hover:opacity-100 transform translate-y-4 group-hover:translate-y-0 transition-all duration-300"></span>
                                    </div>
                                </button>
                            ))}
                        </div>
                    )}
                </div>
            </div>
        )}
    </div>

    <!-- Lightbox Modal -->
    <dialog id="image-lightbox" class="backdrop:bg-black/90 backdrop:backdrop-blur-sm bg-transparent p-4 m-auto max-w-[95vw] max-h-[95vh] w-full h-full rounded-none shadow-none open:animate-fade-in z-[100] outline-none">
        <div class="relative w-full h-full flex items-center justify-center">
            <form method="dialog" class="absolute top-0 right-0 z-50">
                <button class="text-white opacity-70 hover:opacity-100 transition-opacity p-2 cursor-pointer bg-black/50 hover:bg-black/80 rounded-full">
                    <div class="i-ri-close-line text-3xl"></div>
                </button>
            </form>
            <img id="lightbox-img" src="" alt="Fullscreen preview" class="max-w-full max-h-[90vh] object-contain rounded-lg shadow-2xl" />
        </div>
    </dialog>

    <script>
        document.addEventListener('astro:page-load', () => {
            const lightbox = document.getElementById('image-lightbox') as HTMLDialogElement;
            const lightboxImg = document.getElementById('lightbox-img') as HTMLImageElement;
            
            document.querySelectorAll('.lightbox-trigger').forEach(trigger => {
                trigger.addEventListener('click', () => {
                    const src = trigger.getAttribute('data-src');
                    if (src && lightbox && lightboxImg) {
                        lightboxImg.src = src;
                        lightbox.showModal();
                    }
                });
            });

            // Close on click outside
            if (lightbox) {
                lightbox.addEventListener('click', (e) => {
                    if (e.target === lightbox) {
                        lightbox.close();
                    }
                });
            }

            // Force video reload for ViewTransitions
            document.querySelectorAll('video').forEach(video => {
                video.load();
                video.play().catch((e) => { console.warn(e); });
            });

            // Force opacity reset on prose P tags if they misbehave
            document.querySelectorAll('.prose p').forEach(el => {
                (el as HTMLElement).style.opacity = '1';
            });
        });
    </script>
</BaseLayout>
